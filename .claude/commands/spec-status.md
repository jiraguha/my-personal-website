List all specs in specs/ (excluding TEMPLATE.md), sorted by number.

For each spec:
1. Number and name
2. Mode (full / prototype)
3. Status
4. API tests exist? Pass/fail?
5. UI tests exist? Pass/fail?
6. E2E tests exist? Pass/fail?

Format as table.

After the table, show:
- Count of complete specs
- Count of prototype specs (warn if any are old — prototypes should be promoted or deleted)
- Which spec is current
- What to do next
